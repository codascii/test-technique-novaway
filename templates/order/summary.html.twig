{% extends 'base.html.twig' %}

{% block body %}
    <h2>Commande #{{ order.id}}</h2>
    <p>Merci pour votre commande</p>
    <table class="table">
        <tr>
            <th scope="row" role="rowheader">Montant</th>
            <td>{{ order.price }} €</td>
        </tr>
    </table>
    <table class="table">
        <tr>
            <th>Addresse de facturation</th>
            <th>Addresse de livraison</th>
        </tr>
        <tr>
            <td>
                {{ app.user.street }}<br>
                {{ app.user.zipcode }} {{ app.user.city }}
            </td>
            <td>
                {{ order.deliveryStreet }}<br>
                {{ order.deliveryZipcode }} {{ order.deliveryCity }}
            </td>
        </tr>
    </table>


{% endblock %}
